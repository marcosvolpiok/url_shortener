services:
  backend:
    build:
      context: .
      target: develop
    image: mesa-v2
    ports:
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: "true"
    command: yarn start:dev
